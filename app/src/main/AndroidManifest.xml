<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.chiigu.testyaotia"
          xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Include following permission if you load images from Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Include following permission if you want to cache images on SD card -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MutiplyFormatLayoutActivity"/>
        <activity android:name=".OptionCircleViewActivity"/>
        <activity android:name=".GestureActivity"/>
        <activity android:name=".OptionCardViewActivity"/>
        <activity android:name=".OptionsCardViewGroupActivity"/>

    </application>

</manifest>